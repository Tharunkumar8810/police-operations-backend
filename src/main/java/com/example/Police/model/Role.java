package com.example.Police.model;

public enum Role {
    OFFICER,      // ground level, does patrolling
    SHO,          // Station House Officer, creates & assigns patrols
    SUPERVISOR    // monitors live operations
}